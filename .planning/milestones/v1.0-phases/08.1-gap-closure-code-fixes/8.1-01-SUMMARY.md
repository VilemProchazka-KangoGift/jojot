---
phase: 08.1-gap-closure-code-fixes
plan: 01
subsystem: ui
tags: [wpf, orphan-recovery, autosave, tab-switch, flush]

# Dependency graph
requires:
  - phase: 08-menus-context-actions
    provides: CreateRecoveryCard method, AutosaveService, TabList_SelectionChanged
provides:
  - Open button in orphan recovery panel (ORPH-03)
  - FlushAsync wired into tab-switch path (EDIT-03)
affects: [orphan-recovery, autosave, tab-management]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "async void event handler with await — established pattern for WPF event handlers requiring async work"
    - "FlushAsync replaces Stop+SaveCurrentTabContent in tab switch — single authoritative save path"

key-files:
  created: []
  modified:
    - JoJot/MainWindow.xaml.cs

key-decisions:
  - "TabList_SelectionChanged made async void (not async Task) — WPF event handler pattern, consistent with [06-01] and [03-02]"
  - "FlushAsync removes need for explicit Stop() call — FlushAsync stops timer internally, updates _lastWriteCompleted for frequency cap"
  - "Open button uses same cleanup pattern as Adopt/Delete: action -> RemoveOrphanGuid -> RefreshAfterOrphanAction"

patterns-established:
  - "Open orphan action: await OpenWindowForOrphanAsync -> RemoveOrphanGuid -> RefreshAfterOrphanAction"

requirements-completed: [ORPH-03, EDIT-03]

# Metrics
duration: 1min
completed: 2026-03-03
---

# Phase 8.1 Plan 01: Gap Closure Code Fixes Summary

**Open button added to orphan recovery panel and FlushAsync wired into tab-switch to preserve write frequency cap state**

## Performance

- **Duration:** 1 min
- **Started:** 2026-03-03T14:35:22Z
- **Completed:** 2026-03-03T14:37:15Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Added Open button between Adopt and Delete in `CreateRecoveryCard` — orphan recovery panel now has all three actions (ORPH-03)
- Open button click handler calls `App.OpenWindowForOrphanAsync(guid)`, then `RemoveOrphanGuid`, then `RefreshAfterOrphanAction` — same cleanup pattern as Adopt/Delete
- Made `TabList_SelectionChanged` async void and replaced `_autosaveService.Stop() + SaveCurrentTabContent()` with `await _autosaveService.FlushAsync()` (EDIT-03)
- Fixed stale docstring on `MenuRecover_Click` (removed "stub" reference)
- Scroll offset preservation and `_checkpointTimer.Stop()` are intact after FlushAsync change

## Task Commits

Each task was committed atomically:

1. **Task 1: Add Open button to orphan recovery panel (ORPH-03)** - `34afcc8` (feat)
2. **Task 2: Wire FlushAsync into tab switch path (EDIT-03)** - `ea0db7f` (feat)

**Plan metadata:** _(pending final commit)_

## Files Created/Modified
- `JoJot/MainWindow.xaml.cs` - Added Open button to `CreateRecoveryCard`; changed `TabList_SelectionChanged` to async void with `FlushAsync`; fixed `MenuRecover_Click` docstring

## Decisions Made
- `TabList_SelectionChanged` is `async void` (not `async Task`) — consistent with WPF event handler pattern established in [06-01], [03-02], [08-02]
- `FlushAsync` internally calls `Stop()` on the timer, so explicit `_autosaveService.Stop()` is removed — no double-stop
- `SaveCurrentTabContent()` removed from tab-switch path — `FlushAsync` saves through the service, avoiding a double write

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing CS4014 warning at line 451 (`Dispatcher.BeginInvoke` for scroll restore) — intentional fire-and-forget per decision [06-02], out of scope per deviation rules

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- ORPH-03 and EDIT-03 gaps closed; v1.0 milestone audit items resolved
- MainWindow.xaml.cs clean — no regressions in scroll offset, checkpoint timer, or double-write scenarios

---
*Phase: 08.1-gap-closure-code-fixes*
*Completed: 2026-03-03*

## Self-Check: PASSED
- JoJot/MainWindow.xaml.cs: FOUND
- .planning/phases/08.1-gap-closure-code-fixes/8.1-01-SUMMARY.md: FOUND
- Commit 34afcc8 (Task 1 - Open button): FOUND
- Commit ea0db7f (Task 2 - FlushAsync): FOUND
- Commit 788af31 (metadata): FOUND
