---
status: resolved
phase: 15.1-recovery-rename-reorder-fixes
source: [15.1-01-SUMMARY.md, 15.1-02-SUMMARY.md]
started: 2026-03-06T12:10:00Z
updated: 2026-03-06T14:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Escape cancels tab rename on first press
expected: Double-click a tab to enter rename mode. Press Escape once. The rename should cancel immediately (restoring the original name) without needing a second press.
result: pass

### 2. Tab drag shows in-place fade (no floating ghost)
expected: Click and drag a tab to reorder. The dragged tab should fade to 50% opacity in its original position. There should be NO floating ghost/adorner following your cursor.
result: issue
reported: "On drag the item flashes for a split second, then returns to the original opacity. After dropping the item in the new position, it appears there faded until I click on another tab. The tab hover effect is still there when doing the drag"
severity: major

### 3. Drop completion fade-in animation
expected: After dragging a tab and releasing, the tab should animate from 50% back to 100% opacity with a smooth ease-in over roughly 150ms. It should not snap back instantly.
result: issue
reported: "Tab stays faded after drop until clicking another tab — fade-in animation not restoring opacity"
severity: major

### 4. Recovery panel shows flat rows (not cards)
expected: If you have orphaned sessions from other virtual desktops, the recovery panel should display them as flat rows with subtle dividers — not as rounded card-style boxes.
result: issue
reported: "pass but the padding is wrong, remove left offset on the list"
severity: cosmetic

### 5. Recovery row shows tab previews
expected: Each recovery row should show: the desktop name (bold), tab count + date (muted), and individual tab lines with the tab name and a content excerpt (italic).
result: issue
reported: "no desktop name"
severity: major

### 6. Recovery panel Adopt and Delete buttons
expected: Each recovery row should have an "Adopt" button on the left and a "Delete" button on the right, allowing you to recover or discard that desktop's tabs.
result: pass

## Summary

total: 6
passed: 2
issues: 4
pending: 0
skipped: 0

## Gaps

- truth: "Tab drag fades to 50% opacity in-place during drag"
  status: resolved
  reason: "User reported: On drag the item flashes for a split second, then returns to the original opacity. Tab hover effect still active during drag."
  severity: major
  test: 2
  root_cause: "LostMouseCapture handler on TabList fires when Mouse.Capture(TabList, SubTree) transfers capture during drag start, immediately resetting opacity to 1.0 and calling CompleteDrag() — aborting the drag within milliseconds"
  artifacts:
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "LostMouseCapture handler (lines 146-157) does not distinguish intentional capture transfer from genuine capture loss"
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Drag start (lines 1398-1411) sets _isDragging=true, Opacity=0.5, then Mouse.Capture(TabList) which triggers LostMouseCapture"
  missing:
    - "Add _isTransferringCapture guard flag around Mouse.Capture(TabList) call and skip LostMouseCapture handler when flag is set"
  debug_session: ".planning/debug/tab-drag-opacity-issues.md"

- truth: "Drop completion animates tab from 50% to 100% opacity"
  status: resolved
  reason: "User reported: Tab stays faded after drop until clicking another tab — fade-in animation not restoring opacity"
  severity: major
  test: 3
  root_cause: "Downstream of test 2 — LostMouseCapture aborts drag immediately so the drop completion animation code path (lines 1570-1592) is never reached. Secondary risk: RebuildTabList() destroys ListBoxItems, and SelectTabByNote triggers TabList_SelectionChanged which could interfere with animation on new items"
  artifacts:
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Drop animation code (lines 1570-1592) unreachable because drag is aborted by LostMouseCapture"
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "RebuildTabList then SelectTabByNote could cause stale item references for animation"
  missing:
    - "Fix test 2 root cause first, then verify animation works end-to-end after drag completes normally"
  debug_session: ".planning/debug/tab-drag-opacity-issues.md"

- truth: "Recovery panel rows have correct padding/alignment"
  status: resolved
  reason: "User reported: padding is wrong, remove left offset on the list"
  severity: cosmetic
  test: 4
  root_cause: "Double left margin — ScrollViewer in XAML has Margin='16,0,16,16' and row container StackPanel adds Margin(12,10,12,10), compounding to 28px left offset vs 16px header alignment"
  artifacts:
    - path: "JoJot/MainWindow.xaml"
      issue: "ScrollViewer Margin='16,0,16,16' (line 798) provides 16px horizontal padding"
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "CreateRecoveryRow container Margin(12,10,12,10) (line 2515) adds another 12px horizontal"
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Divider Margin(12,0,12,0) (line 2665) also compounds with ScrollViewer margin"
  missing:
    - "Remove horizontal margin from row container — change Margin(12,10,12,10) to Margin(0,10,0,10)"
    - "Remove horizontal margin from divider — change Margin(12,0,12,0) to Margin(0,0,0,0)"
  debug_session: ""

- truth: "Recovery row shows desktop name (bold)"
  status: resolved
  reason: "User reported: no desktop name"
  severity: major
  test: 5
  root_cause: "Code does render nameBlock with desktopName but either (a) desktop_name is null in app_state for orphaned sessions created during COM fallback mode, showing 'Unknown desktop' instead of actual name, or (b) FontWeights.SemiBold at size 13 is not visually prominent enough vs other text"
  artifacts:
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "nameBlock uses FontWeights.SemiBold (not Bold) at FontSize 13 — may not look distinct"
    - path: "JoJot/Services/DatabaseService.cs"
      issue: "GetOrphanedSessionInfoAsync returns null desktop_name if not stored"
    - path: "JoJot/Services/VirtualDesktopService.cs"
      issue: "Fallback mode creates session with null name"
  missing:
    - "Change FontWeights.SemiBold to FontWeights.Bold and increase FontSize to 14 for desktop name"
    - "Investigate whether desktop_name is actually null for test orphaned sessions"
  debug_session: ""
