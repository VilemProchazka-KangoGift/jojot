---
status: resolved
phase: 15.1-recovery-rename-reorder-fixes
source: [15.1-03-SUMMARY.md, 15.1-04-SUMMARY.md]
started: 2026-03-06T14:30:00Z
updated: 2026-03-06T16:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Tab drag shows 50% opacity fade during drag
expected: Click and drag a tab to reorder. The dragged tab should fade to 50% opacity in its original position and STAY faded while you hold the mouse button. No flash/snap back. No floating ghost. Hover effect should not activate during drag.
result: issue
reported: "fail. the fade stays until I move to another tab, then it reverts back to 100%. after drop it goes to fade again and stays there like last time. Hover effect is still there as well."
severity: major

### 2. Drop completion fade-in animation
expected: After dragging a tab and releasing, the tab should animate smoothly from 50% back to 100% opacity over ~150ms. It should NOT stay faded or snap instantly to full opacity.
result: issue
reported: "fail. same as test 1 — tab stays faded after drop"
severity: major

### 3. Recovery panel row padding alignment
expected: Open recovery panel (need orphaned sessions from other desktops). Rows should be flush-aligned with the panel header — no extra left offset/indentation. Dividers should span the full width within the panel padding.
result: issue
reported: "fail. the bullet list offset is still there"
severity: cosmetic

### 4. Recovery row shows desktop name prominently
expected: Each recovery row should show the desktop name in bold at a clearly visible size (larger than the metadata text below it). It should NOT say "Unknown desktop" if the desktop had a name.
result: issue
reported: "fail"
severity: major

## Summary

total: 4
passed: 0
issues: 4
pending: 0
skipped: 0

## Gaps

- truth: "Tab drag fades to 50% opacity in-place and stays faded during drag"
  status: resolved
  reason: "User reported: fade stays until moving to another tab, then reverts to 100%. After drop goes to fade again and stays. Hover effect still active during drag."
  severity: major
  test: 1
  root_cause: "WPF animation local-value reversion: CompleteDrag() line 1591 sets item.Opacity=0.5 as local value. When fade-in animation completes and clock is removed via BeginAnimation(null), WPF reverts to the local value 0.5 instead of default 1.0. Tab snaps from 1.0 back to 0.5 permanently. Additionally, pinBtn and closeBtn hover handlers (lines 354-382, 454-458) lack _isDragging guards."
  artifacts:
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Line 1591: item.Opacity = 0.5 sets local value that persists after animation clock removal"
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Line 1598: fadeIn.Completed handler clears animation clock, exposing stale local value of 0.5"
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Lines 354-382, 454-458: pinBtn/closeBtn hover handlers missing _isDragging guards"
  missing:
    - "Remove line 1591 (item.Opacity = 0.5) — the From=0.5 on DoubleAnimation handles initial value"
    - "Or: set item.Opacity = 1.0 in Completed handler BEFORE calling BeginAnimation(null)"
    - "Add if (_isDragging) return; guards to pinBtn and closeBtn MouseEnter/MouseLeave handlers"
  debug_session: ".planning/debug/tab-drag-ghost-still-broken.md"

- truth: "Drop completion animates tab from 50% to 100% opacity"
  status: resolved
  reason: "User reported: same as test 1, tab stays faded after drop"
  severity: major
  test: 2
  root_cause: "Downstream of test 1 — same animation reversion bug. When animation completes, local value 0.5 is exposed, making tab appear to stay faded."
  artifacts:
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Same as test 1 — line 1591 local value + line 1598 animation clock removal"
  missing:
    - "Fixed by same change as test 1"
  debug_session: ".planning/debug/tab-drag-ghost-still-broken.md"

- truth: "Recovery panel rows flush-aligned with header, no extra left offset"
  status: resolved
  reason: "User reported: bullet list offset is still there"
  severity: cosmetic
  test: 3
  root_cause: "Tab preview TextBlocks have Margin(8,1,0,1) at lines 2561 and 2603 creating 8px left indent. 15.1-04 fix zeroed container margin but missed child element margins. ScrollViewer provides 16px padding, desktop name sits at 16px, but tab lines sit at 24px (16+8)."
  artifacts:
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Line 2561: tab preview Margin = new Thickness(8, 1, 0, 1) — 8px left indent"
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Line 2603: '+N more' Margin = new Thickness(8, 1, 0, 1) — same 8px indent"
  missing:
    - "Change both margins from (8, 1, 0, 1) to (0, 1, 0, 1)"
  debug_session: ""

- truth: "Recovery row shows desktop name in bold at visible size"
  status: resolved
  reason: "User reported: fail (desktop name not visible/prominent)"
  severity: major
  test: 4
  root_cause: "Bold 14pt styling IS applied, but desktop_name is null in DB for orphaned sessions. GetOrphanedSessionInfoAsync returns null name, causing fallback to 'Unknown desktop'. Orphaned sessions never get names updated because they're unmatched. Registry fallback GetDesktopNameFromRegistry() exists but is private and never called during orphan info retrieval."
  artifacts:
    - path: "JoJot/MainWindow.xaml.cs"
      issue: "Line 2532: Text = desktopName ?? 'Unknown desktop' — fallback when name is null"
    - path: "JoJot/Services/DatabaseService.cs"
      issue: "Lines 870-876: GetOrphanedSessionInfoAsync returns null desktop_name from DB"
    - path: "JoJot/Services/VirtualDesktopService.cs"
      issue: "Line 169: Fallback mode creates sessions with null name; GetDesktopNameFromRegistry is private"
  missing:
    - "Expose or duplicate registry name lookup for orphaned desktop GUIDs"
    - "In CreateRecoveryRow or GetOrphanedSessionInfoAsync, attempt registry lookup when desktop_name is null"
  debug_session: ""
