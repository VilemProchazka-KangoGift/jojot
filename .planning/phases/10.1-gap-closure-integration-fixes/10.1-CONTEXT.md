# Phase 10.1: Gap Closure — Integration Fixes - Context

**Gathered:** 2026-03-03
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix 3 integration findings from the v1.0 milestone audit and correct 1 documentation artifact. All fixes are hardening of already-satisfied requirements — no new capabilities.

1. OnClosing must commit pending deletions before geometry save
2. ShowDragOverlayAsync must FlushAsync before InsertPendingMoveAsync
3. FlushAndClose must await CommitPendingDeletionAsync instead of fire-and-forget
4. THME-02 checkbox in REQUIREMENTS.md changed from `[ ] Pending` to `[x] Complete`

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion

All implementation details are at Claude's discretion. The audit prescribes the exact fix locations and approach:

**Fix 1 — OnClosing pending deletion commit:**
- Location: `MainWindow.xaml.cs:OnClosing` (line ~1650)
- Add `CommitPendingDeletionAsync().GetAwaiter().GetResult()` before geometry save
- Error handling strategy and placement within the method are Claude's call

**Fix 2 — FlushAsync before drag detection:**
- Location: `MainWindow.xaml.cs:ShowDragOverlayAsync` (line ~3186)
- Add `await _autosaveService.FlushAsync()` before `InsertPendingMoveAsync`
- Whether to add any guard or optimization is Claude's call

**Fix 3 — FlushAndClose await:**
- Location: `MainWindow.xaml.cs:FlushAndClose` (line ~1627)
- Change `_ = CommitPendingDeletionAsync()` to proper await
- How to coordinate with `ExitApplication` and `Environment.Exit(0)` is Claude's call

**Fix 4 — THME-02 checkbox:**
- Location: `.planning/REQUIREMENTS.md`
- Change THME-02 from `[ ] Pending` to `[x] Complete`
- Pure documentation fix

</decisions>

<specifics>
## Specific Ideas

No specific requirements — the v1.0 audit prescribes exact fixes with suggested code patterns. Follow the audit recommendations.

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `CommitPendingDeletionAsync()`: Already exists in MainWindow.xaml.cs — hard-deletes pending tabs from DB
- `_autosaveService.FlushAsync()`: Already exists — used at tab switch points, proven pattern
- `DatabaseService.UpdateNoteContentAsync()`: Sync flush pattern via `.GetAwaiter().GetResult()` already used in OnClosing

### Established Patterns
- Synchronous flush on close: `OnClosing` already uses `.GetAwaiter().GetResult()` for content save — same pattern applies for deletion commit
- All 3 fixes are in `MainWindow.xaml.cs` — single-file change
- `FlushAndClose` is called from `ExitApplication` which loops all windows then calls `Environment.Exit(0)`

### Integration Points
- `OnClosing` → `CommitPendingDeletionAsync` (new call, before geometry save)
- `ShowDragOverlayAsync` → `_autosaveService.FlushAsync` (new call, before InsertPendingMoveAsync)
- `FlushAndClose` → `CommitPendingDeletionAsync` (change from fire-and-forget to sync)
- `ExitApplication` → `FlushAndClose` (may need signature adjustment if FlushAndClose changes)

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 10.1-gap-closure-integration-fixes*
*Context gathered: 2026-03-03*
